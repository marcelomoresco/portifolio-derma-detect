import 'package:flutter/material.dart';

class AnalysisDetailTemplate extends StatelessWidget {
  const AnalysisDetailTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
